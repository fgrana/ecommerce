export class CardClass {
  name: string;
  atk: number;
  def: number;
  spd: number;
  lif: number;
  constructor(name: string, atk: number, def: number, spd: number, lif: number) {
    this.name = name;
    this.atk = atk;
    this.def = def;
    this.spd = spd;
    this.lif = lif;
  }

  // Puedes agregar métodos adicionales aquí si es necesario
}

